# Client Reports

Each subdirectory contains the marketing strategy for one prospect.

## Structure
clients/{domain}/MarketingStrategy_{domain}_{date}.md — Full strategy
clients/{domain}/README.md — Client summary card

Reports are generated automatically by ProspectorAI.
